import "./style.css";
window.Buffer = window.Buffer || require("buffer").Buffer;

const PageSolana = () => {
  return (
    <div style={{ padding: "30px" }}>
      <h1 className="title">Solana</h1>
    </div>
  );
};

export default PageSolana;
